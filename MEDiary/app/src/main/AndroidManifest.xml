<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.katharinafeiertag.mediary">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:logo="@drawable/logo"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:label="@string/title_activity_login"
            android:name=".HauptmenuActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!--<activity WENN LOGIN STARTSEITE IST
            android:label="@string/title_activity_login"
            android:name=".LoginActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->

        <activity android:name=".BenutzerprofilActivity" />
        <activity android:name=".RegistrierenActivity" />
        <activity android:name=".HausapothekeActivity" />
        <activity
            android:name=".EinstellungenActivity"
            android:label="@string/title_activity_einstellungen" />
        <activity android:name=".AptknaeheActivity" />
        <activity android:name=".EinnahmeActivity" />
        <!--<activity android:name=".HauptmenuActivity"></activity>-->


    </application>

</manifest>